<div class="container">
  <div class="row">
    <div class="title">
      <h1>Rocket Ships</h1>
    </div>
  </div>

  <div class="row">
    <%# <div class="search"> %>
      <%= form_tag rockets_path, method: :get do %>
        <div class="search-form-control form-group">
          <%= text_field_tag :query,
            params[:query],
            class: "form-control",
            placeholder: "Find a rocket"
          %>
          <%= submit_tag "Search", class: "btn btn-ghost" %>
      <% end %>
    </div>
  </div>
      
      <%# <% if @query %>
        <%# <p>You searched for <strong><%= @query</strong>. We found <%= @rockets.count rockets out of our <%= Rocket.count on our website.</p> %>
      <%# <% else
        <%# <p>All <%= Rocket.count rockets.</p> %>
      <%# <% end %>
  <div class="index-body">
    <div class="cards">
      <% @rockets.each do |rocket| %>
        <div class="rocket-card">
          <%= cl_image_tag rocket.photo.key, height: 120, width: 120 if rocket.photo.attached? %>
          <div class="rocket-card-content">
            <div class="rocket-basic-info">
              <h5><%= rocket.name %></h5>
              <p><%= rocket.description %></p>
            </div>
            <div class="rocket-capacity-price">
              <i class="material-icons mr-2">Capacity</i><span><%= rocket.capacity %></span>
              <i class="material-icons mr-2">£</i><%= rocket.price %>
            </div>
            <div class="rocket-actions">
              <%= link_to 'More info', rocket_path(rocket) %>
            </div>   
         </div>
       </div>
      <% end %>
    </div>
  </div>
</div>



  <%# <div class="row">
    <% @rockets.each do |rocket| %>
      <%# <div class="col-4">
        <div class="rocket-card">
          <div class="rocket-card-image" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path(rocket.photo.key) if rocket.photo.attached? %>
            <%# <div class="rocket-card-content">
              <div class="rocket-card-title">
                <%# <h4><%= rocket.name</h4> %>
              <%# </div>
              <%# <p><%= rocket.description</p> %>
              <%# <div class="flex">
                <div class="flex"> %>
                  <%# <i class="material-icons mr-2">Capacity</i><span><%= rocket.capacity</span> %>
                <%# </div> %>
                <%# <div class="flex ml-3"> %>
                  <%# <i class="material-icons mr-2">£</i> <%= rocket.price %>
                <%# </div> %>
              <%# </div> %>
            <%# </div> %>
          <%# </div> %>
          <%# <div class="rocket-card-actions"> %>
            <%# <%= link_to 'More info', rocket_path(rocket) %>
            <%# <%= link_to 'Edit', edit_rocket_path(rocket), class: 'ml-3 text-warning' %>
            <%# <%= link_to "Remove", rocket_path(rocket),
                method: :delete,
                data: { confirm: "Are you sure?" },
                class: 'ml-3 text-danger'
            %>
          <%# </div> %>
        <%# </div> %>
      <%# </div> %>
    <%# <% end %>
  <%# </div> %>
<%# </div> %>
